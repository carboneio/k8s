apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "carbone-ee.fullname" . }}
spec:
  minAvailable: 1          # ou maxUnavailable: 1
  selector:
    matchLabels:
      {{- include "carbone-ee.selectorLabels" . | nindent 6 }}